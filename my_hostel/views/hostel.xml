<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Tree View -->
    <record id="view_hostel_tree" model="ir.ui.view">
        <field name="name">hostel.hostel.tree</field>
        <field name="model">hostel.hostel</field>
        <field name="arch" type="xml">
            <tree>
                <field name="name"/>
                <field name="hostel_code"/>
                <field name="state_id"/>
            </tree>
        </field>
    </record>

    <!-- Form View -->
    <record id="view_hostel_form" model="ir.ui.view">
        <field name="name">hostel.hostel.form</field>
        <field name="model">hostel.hostel</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <group>
                        <field name="name"/>
                        <field name="hostel_code"/>
                        <field name="street"/>
                        <field name="street2"/>
                        <field name="state_id"/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Action -->
    <record id="action_hostel" model="ir.actions.act_window">
        <field name="name">Hostels</field>
        <field name="res_model">hostel.hostel</field>
        <field name="view_mode">tree,form</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create your first hostel record.
            </p>
        </field>
    </record>

    <!-- Menu Items -->
    <menuitem id="menu_hostel_root" name="Hostel Management" sequence="1"/>
    <menuitem id="menu_hostel" name="Hostels" parent="menu_hostel_root" action="action_hostel" sequence="1"/>

</odoo>
